import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  headText: {
    fontWeight: 'bold',
    fontSize: 32,
    textAlign: 'center',
  },
  header: {
    flex: 2,
  },
  message: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'red',
    textAlign: 'center',
  },
});
